class Map
{
    constructor(height, width)
    {
        this.height = height;
        this.width = width;
    }

    setMapSize(myHeight, myWidth)
    {
        this.height = myHeight;
        this.width = myWidth;

        return size = [this.height, this.width];
    }

    getMapSize()
    {
        return size = [this.height, this.width];
    }
}